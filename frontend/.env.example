# Leave unset for local dev — Vite's dev server proxies "/api" to
# http://localhost:5000 automatically (see vite.config.js).
#
# For production (Vercel), set this to your deployed Render API,
# including the "/api" suffix:
# VITE_API_URL=https://assetflow-backend.onrender.com/api
VITE_API_URL=
